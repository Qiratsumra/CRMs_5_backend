apiVersion: v1
kind: Service
metadata:
  name: fte-api-service
  namespace: customer-success-fte
  labels:
    app: fte-api
spec:
  type: ClusterIP
  selector:
    app: fte-api
  ports:
  - name: http
    port: 80
    targetPort: 8000
    protocol: TCP
